using UnityEngine;
using System.Collections;

public class Button_press : MonoBehaviour {
	public bool checker;
	public Vector3 buttonUp;
	public Vector3 buttonDown;


	// Use this for initialization
	void Start () {/*
		if (this.gameObject.tag ==("Right")){
			bool checker=PlayerController.right;}
		else if (this.gameObject.tag ==("Left")){
			bool checker=PlayerController.left;}
		else if (this.gameObject.tag ==("Fire")){
			bool checker=PlayerController.fire;}

		Vector3 buttonUp= new Vector3( this.transform.position.x, (this.transform.position.y), 0f);
		print(buttonUp);
		Vector3 buttonDown =  new Vector3( this.transform.position.x, (this.transform.position.y - 0.03f), 0f);





	
	}
	
	// Update is called once per frame
	void Update () {
		if (checker=true){
			this.transform.position=buttonDown;}
		else {this.transform.position=buttonUp;}*/



	
	}
}
